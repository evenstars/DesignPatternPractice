package controller;

/**
 * Interface for MLController, has only calculate method for drawing line.
 */
public interface MLController {

  /**
   * Calculate the machine learning algorithm parameters.
   */
  void calculate();
}
